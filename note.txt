✅ Core Features Completed

1. User Registration & Login Authentication
  JWT-based authentication system
  Secure password encryption with BCrypt
  Register and login endpoints
  Role-based access control (USER and ADMIN roles)

2. Product CRUD Operations
  Create, Read, Update, Delete products
  Search products by name
  Filter by category
  Get active products
  Stock management
  Admin-only access for modifications

3. Order Placement Functionality
  Create orders with multiple items
  Automatic stock deduction
  Order tracking with unique order numbers
  Order status management (PENDING, CONFIRMED, PROCESSING, SHIPPED, DELIVERED, CANCELLED)
  Order cancellation with stock restoration
  User-specific order history

📦 Project Structure
The application follows a clean, layered architecture:
  Controllers: REST API endpoints
  Services: Business logic
  Repositories: Data access layer
  Entities: JPA models (User, Product, Order, OrderItem)
  DTOs: Request/Response objects
  Security: JWT authentication with Spring Security
  Exception Handling: Global error handling

🚀 How to Run
The API will be available at: http://localhost:8080/api

📝 Key Files Created
  pom.xml - Maven dependencies
  application.yml - Configuration
  Entity models, repositories, services, and controllers
  JWT token provider and authentication filter
  Security configuration
  Global exception handler
  API documentation and examples
The application uses H2 in-memory database for development and is ready to switch to MySQL for production.
All endpoints are documented in the API_EXAMPLES.http file with example requests!
